import { test } from '@playwright/test'

test.skip('admin can open Club Analysis from the dashboard and view persisted detail', async () => {
  // Intentionally skipped until the E2E suite gets a stable fixture reset for daily snapshots.
})
